language: python
python:
  - 2.7
  - 3.7
build:
  pre_ci_boot:
    image_name: drydock/u16pytall
    image_tag: master
    pull: false
    options: --privileged=false --net=bridge
  ci:
   - shippable_retry pip install -Ur test-requirements.txt

   - pytest
   - codecov -t 272fc7a8-bd55-4e0c-af2f-7d475d0f3c0a
